# 匿名阅卷与身份字段

匿名阅卷是 Examark 的核心权限规则之一。

## 字段分层

基础身份字段：

- `student_id`。
- `student_name`。
- `student_no`。
- `classroom_name`。

扫描绑定字段：

- `answer_sheet_id`。
- `barcode`。
- `seat_no`。
- `binding_status`。
- `full_scan_image`。

敏感字段：

- 身份证号。
- 电话。
- 邮箱。
- 国籍。
- 家长信息。
- 地址。

## 匿名 ID

系统区分两类匿名 ID：

- `anonymous_submission_id`：同一学生在本场考试中的匿名整卷记录。
- `anonymous_answer_id`：某一道题的匿名答案记录。

二者都必须不可推断，不能由 `student_id` 推出，也不能跨考试关联。

## 阅卷默认匿名

普通阅卷人和 AI 教师默认只能看到：

- 被分配题目的题干。
- 评分标准。
- 匿名答题内容。
- 识别文本。
- 安全题目裁剪图。

默认不能看到学生身份、学号、班级、整卷原图或答题卡绑定关系。

## 去匿名能力

`can_deanonymize_grading` 是高风险能力。默认仅 owner、examiner、超管或被明确放权者拥有。scanner 虽然能在扫描流程看身份和原卷，但不自动拥有阅卷界面去匿名能力。

## 字段可见性参考

| 场景 | 姓名/学号 | 班级 | 扫描原卷 | 敏感字段 | 说明 |
| --- | --- | --- | --- | --- | --- |
| 扫描绑定 | 可按需要显示 | 可按需要显示 | 可显示 | 默认不显示 | 用于人工绑定和异常卷处理 |
| 普通主观阅卷 | 默认不显示 | 默认不显示 | 默认不显示 | 不显示 | 保持匿名阅卷 |
| 客观复核 | 默认不显示 | 默认不显示 | 按权限裁剪 | 不显示 | 只处理识别答案和分数 |
| 成绩查看 | 按权限显示 | 按权限显示 | 默认不显示 | 不显示 | 成绩发布不等于开放身份 |
| 成绩导出 | 按权限导出 | 按权限导出 | 不导出 | 不导出 | 导出同样执行字段裁剪 |
| 管理员排查 | 按授权显示 | 按授权显示 | 按授权显示 | 按授权显示 | 仅限有明确管理权限的人员 |

## 判断是否应该开放身份字段

可以用三个问题判断：

1. 当前任务是否必须知道学生是谁？
2. 是否可以用匿名 ID 完成任务？
3. 开放姓名、学号或原卷是否会影响阅卷公平或泄露隐私？

如果答案不明确，优先保持匿名，再由考试创建人或学校管理员按需放权。
