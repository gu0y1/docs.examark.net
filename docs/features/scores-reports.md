# 成绩与报告

成绩与报告模块用于汇总考试结果、导出数据和查看学生报告。

## 成绩表

成绩表展示客观分、主观分、调整分、总分和状态。可见字段会根据当前账号 capability 裁剪。

## 导出

支持 CSV 和 Excel 导出。导出不是前端截图，而是后端生成，因此同样执行身份字段裁剪。

## 学生报告

学生报告用于查看单个学生在本场考试中的表现。只有具备成绩查看权限且符合身份字段规则的账号才能看到相应信息。

## 发布状态

成绩状态分为：

- `draft`：草稿。
- `published`：已发布。

发布后开放成绩查看范围，但不自动开放学生身份字段。

